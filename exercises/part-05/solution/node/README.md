# Module 5 — Node + TypeScript Reference Solution (Tests)

Vitest suite for the Module 4 Notes API (Node track).

## Install

```bash
npm install
```

## Run

```bash
npm test
```

The suite imports the Hono `app` from `../part-04/solution/node` and exercises it via `app.request` (no real network).
